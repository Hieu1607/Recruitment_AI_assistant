services:
  # Production override only for the frontend runtime; other services still inherit from docker-compose.yml.
  frontend:
    build:
      context: ./frontend
      target: production
      args:
        VITE_API_BASE_URL: /api/v1
    command: nginx -g 'daemon off;'
    volumes: !reset []
