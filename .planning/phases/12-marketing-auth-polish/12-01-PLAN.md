---
phase: 12-marketing-auth-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/landing.tsx
  - frontend/src/routes/login.tsx
autonomous: true
requirements:
  - MKTG-01
  - MKTG-02
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "User visits `/` while logged out and sees the landing page: 72-96px serif hero ('Hire like it's 2030.'), 4-column value strip with line icons, browser-frame product showcase, 3-4 alternating left/right feature deep-dives with screenshots, social-proof logo bar with italicized testimonial, big-CTA closer, and editorial footer"
    - "User clicks 'Get started', lands on the split-screen Auth page (60% editorial accent panel + 40% form), toggles between Sign in / Sign up"
    - "Types an invalid email and the field shake-animates with inline validation"
    - "Clicking 'Sign in' shows a clear 'auth not yet enforced' UI hint"
  artifacts:
    - path: "frontend/src/routes/landing.tsx"
      provides: "Landing page UI"
    - path: "frontend/src/routes/login.tsx"
      provides: "Split-screen Auth UI with validation"
---

<objective>
Build the Marketing Landing Page and Authentication UI.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Landing Page</name>
  <files>frontend/src/routes/landing.tsx</files>
  <action>
  Implement the Landing Page route.
  Requirements:
  - Serif hero section.
  - 4-column value strip.
  - Feature deep-dives with placeholders for screenshots.
  - Social-proof section.
  - Footer and CTAs linking to `/login`.
  </action>
</task>
<task type="auto">
  <name>Task 2: Auth UI</name>
  <files>frontend/src/routes/login.tsx</files>
  <action>
  Implement the Auth route (used for both Sign In and Sign Up).
  Requirements:
  - Split screen: 60% left accent panel with editorial text/quotes, 40% right side form.
  - Toggle between sign in and sign up.
  - Client-side validation: shake animation on invalid email.
  - Mock submit that redirects to `/dashboard` (or `/` when authenticated) and shows a toast: "Auth not yet enforced — welcome!".
  </action>
</task>
</tasks>
