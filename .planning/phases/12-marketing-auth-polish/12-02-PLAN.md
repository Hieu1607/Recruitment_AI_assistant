---
phase: 12-marketing-auth-polish
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/routes/settings.tsx
  - frontend/src/components/CommandPalette.tsx
  - frontend/src/routes/_app-shell.tsx
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
must_haves:
  truths:
    - "User presses ⌘K from any authenticated screen, the command palette opens with fuzzy search across candidates, JDs, collections, and core actions plus a recent-items list; selecting an item navigates or fires the action"
    - "User opens Settings via the avatar dropdown, navigates the left tabs (Profile, Workspace, API keys, Notifications, Danger zone)"
    - "The avatar dropdown exposes Profile / Settings / theme toggle / Sign out"
  artifacts:
    - path: "frontend/src/routes/settings.tsx"
      provides: "Settings page UI"
    - path: "frontend/src/components/CommandPalette.tsx"
      provides: "Cmd+K command palette"
---

<objective>
Build the Settings page and global Command Palette (Cmd+K).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Settings Page</name>
  <files>frontend/src/routes/settings.tsx</files>
  <action>
  Implement the Settings route.
  Requirements:
  - Sidebar with tabs: Profile, Workspace, API keys, Notifications, Danger zone.
  - Form UI for each tab.
  </action>
</task>
<task type="auto">
  <name>Task 2: Command Palette and TopBar updates</name>
  <files>
    - frontend/src/components/CommandPalette.tsx
    - frontend/src/routes/_app-shell.tsx
  </files>
  <action>
  Implement the global Command Palette (`cmdk` or custom modal) triggered by Cmd+K or Ctrl+K.
  Requirements:
  - Listen for Cmd+K.
  - Search core actions (Go to Dashboard, Add Candidate, New JD, etc.).
  - Integrate with AppShell so it's globally available.
  - Update TopBar Avatar Dropdown to link to Settings, Profile, Theme Toggle, Sign Out.
  </action>
</task>
</tasks>
