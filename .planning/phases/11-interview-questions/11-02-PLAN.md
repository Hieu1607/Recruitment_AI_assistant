---
phase: 11-interview-questions
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/src/routes/interview-questions/detail.tsx
autonomous: true
requirements:
  - INTV-03
  - INTV-04
  - INTV-05
  - INTV-06
must_haves:
  truths:
    - "On the detail page the user sees the serif header 'Interview for [Candidate] — [JD Title]'"
    - "Questions grouped by category (Technical / Behavioral / Culture-fit)"
    - "Each card with serif quote-style question text, category + difficulty badges, and an editable notes textarea"
    - "User drags a question from Technical into Behavioral via @dnd-kit, edits its text inline, adds a new question via '+ Add question'"
    - "Clicking Save persists the updated question_payload via PATCH /interview-questions/{id}"
    - "User clicks 'Export as PDF' (or Print) and a print-optimized layout renders all questions cleanly without sidebar/topbar chrome"
  artifacts:
    - path: "frontend/src/routes/interview-questions/detail.tsx"
      provides: "Detail page with @dnd-kit reordering and print styling"
---

<objective>
Build the detail page for Interview Questions.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Interview Questions Detail and DND</name>
  <files>frontend/src/routes/interview-questions/detail.tsx</files>
  <action>
  Implement the InterviewQuestionsDetail route.
  Requirements:
  - Fetch question set via `api.interviewQuestions.get(id)`.
  - Group questions by category. Add `@dnd-kit` to allow dragging to reorder or move between categories.
  - Render an inline text editor for questions, and a textarea for notes.
  - "Save" button triggers `api.interviewQuestions.update(id, { question_payload: newPayload })`.
  - Add print specific CSS (`@media print`) so the layout is clean when printing/exporting.
  </action>
</task>
</tasks>
